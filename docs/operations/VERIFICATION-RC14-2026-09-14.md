# RC14 verification — 2026-09-14

## Scope

This record covers the completed RC14 code, automated checks, isolated PostgreSQL checks, Android emulator matrix, Docker recovery, encrypted restore drill, signed publication, and selected production health checks. It does not claim rotated Gmail credentials, configured FCM delivery, owner MFA re-enrollment, HSTS, or a complete physical phone/watch end-to-end run.

## API and web automated evidence

- Repository-wide Ruff and strict mypy passed for `services/api` and `services/ai`.
- Twenty-two focused, database-independent security tests passed, including rejection of spoofed or malformed gateway client-address headers and proof that duplicate/recovery paths execute their neutral password work.
- RC14 integration tests passed against disposable PostgreSQL databases after clean Alembic upgrades from `0001` through current head `0023`.
- Migration `0017` also passed a clean-schema upgrade, downgrade to `0016`, and re-upgrade rehearsal.
- The integration cases covered concurrent capped and expiring counters, neutral duplicate registration, serialized verification and recovery cooldowns, full reset-token consumption, a reset-versus-session-rotation race, deleted-account login rejection, MFA replacement proof and session revocation, configuration allowlisting, suspended-partner confirmation rejection, competing pair confirmations, concurrent unpairing and account deletion, legacy export reveal, and revoked-session note mutation.
- The final database-independent Python run passed 139 tests with 31 destructive or environment-gated cases skipped. Ruff passed repository-wide and strict mypy passed all 149 source files. Separate disposable PostgreSQL cases proved that both declared-expiry and absolute-24-hour predicates delete raw coordinates and that a newly created note, edits from both partners, and a reconnect snapshot converge without exposing either note to another couple. The privacy and deletion cycle was extracted into a focused maintenance module after the quality ratchet found the worker entry point above its 500-logical-line ceiling; the final 405-file size and complexity gate passes.
- A gateway-level paired smoke created a document as partner A, listed it as partner B, delivered the same revision to both authenticated sockets, caught up after partner B disconnected, and accepted the reverse edit from partner B. The per-installation smoke also passed content-free hints, independent Smooch acknowledgement, and the 30-minute note-alert cooldown.
- The owner enrollment form passed three focused replacement-proof tests, all 33 web unit tests, lint, strict TypeScript, the production build, and 18 desktop/mobile Playwright checks including its first-enrollment and replacement states.
- The documentation inventory and Markdown lint passed, as did the repository-wide source-size and complexity guard.

### Web release presentation follow-up

- A read-only check of the deployed RC13 route found that complete Markdown release documents were being split into literal list rows. Heading markers, list markers, emphasis, and code ticks therefore appeared as text, and long artifact hashes widened the page on a phone viewport.
- The local RC14 web build now normalizes stored Markdown into plain release rows, limits the download card to five highlights, keeps the complete normalized history on `/patch-notes` and in RSS, formats publication dates in UTC, and permits long immutable values to wrap. The owner release form reads the published phone, Wear, and compatibility-floor version codes instead of presenting stale RC9-era defaults.
- `npm run check` passed after the fix: zero-warning ESLint, strict TypeScript, 33 Vitest cases, and the Next.js production build. All 18 desktop/mobile Playwright cases also passed.
- In the in-app browser, the live-backed local download route rendered exactly five clean RC13 highlights at 397 CSS pixels with document width equal to viewport width. The patch-notes route retained all 15 normalized RC13 rows, used `Sep 14, 2026` consistently, contained no literal Markdown heading row, had no horizontal overflow, navigated from Download, and logged no warning or error. An unauthenticated `/admin` request redirected to `/admin/login`; an earlier forged-cookie case passed in both Playwright projects.
- Read-only production checks confirmed RC13 phone version code 18, Wear version code 15, compatibility floor 6, matching phone/Wear sizes and SHA-256 values, RSS `application/rss+xml`, and a 1,024-byte APK request returning `206`, `Accept-Ranges: bytes`, the exact total length, immutable cache headers, checksum header, and matching ETag. No production record, container, proxy, artifact, or account was changed.
- After authorized RC14 publication, the web fallback was advanced to the same verified RC14 metadata and the container was rebuilt. Public `/download` now names RC14 and links directly to its first-party APK route; `/patch-notes` and RSS include RC14 without transient pre-publication status prose. The web lint, strict TypeScript, 33 Vitest cases, and production build passed before deployment.

The reviewed web container is deployed behind the existing gateway. Local and public `/download`, `/patch-notes.xml`, API liveness, and release-metadata requests returned `200`; the web response carries the strict CSP. HSTS remains intentionally disabled until the NPM certificate-renewal and recovery gate is completed.

The disposable database was isolated from development and hosted data and was removed after the run. No live secret, account, proxy, or production database was changed.

## Android automated and emulator evidence

- `data:testDebugUnitTest`, `mobile:testDebugUnitTest`, `widget:testDebugUnitTest`, and `wear:testDebugUnitTest` passed with focused coverage for relationship identity and generation ordering, stale and exact 24-hour boundaries, unavailable display wording, compact widget selection, delayed payload rejection, legacy replay, purge barriers, manual installer precedence, transient pairing-code redaction, watch compatibility, and installed-version policy. A delayed authorization record could not revive an expired watch cache, and an implausibly future-dated record could not extend retention. The shared bounded service-error parser accepted only the exact structured `relationship_inactive` code; generic, message-only, malformed, oversized, and unrelated `409` responses did not invalidate relationship caches.
- A final combined 199-task Gradle run passed all four debug unit suites, mobile/widget/Wear debug lint, and debug phone and Wear APK assembly. No Android module version or signing file was changed in this slice.
- A later visual/accessibility follow-up passed 193 Gradle tasks covering mobile, widget, and Wear unit tests, debug lint, and debug phone/Wear assembly. Mobile lint dropped from 149 to 104 warnings after hardcoded text, autofill, text-field, programmatic concatenation, monochrome-icon, and redundant-layout findings were resolved; widget lint dropped from ten warnings to two after passive text sizing and plural accessibility wording were repaired; Wear lint reached zero after localized shared presentation, guarded receiver actions, and representative 400 px square/round tile previews. The remaining mobile warnings are 74 unused resources, twelve synchronous-preference suggestions, eight version-catalog suggestions, three wireless-ADB trust-manager findings, two plural candidates in separately owned notification/attachment presentation, and one each for backup rules, dependency age, SDK targeting, and a version-qualified permission attribute. The two widget findings concern an API 31 preview attribute and a compound-drawable optimization. There are no remaining `HardcodedText`, `Autofill`, `TextFields`, `SetTextI18n`, `SmallSp`, `MonochromeLauncherIcon`, or Wear lint findings.
- A disposable round API 34 Wear emulator cold-launched the debug Wear APK with synthetic relationship data. Fresh, more-than-six-hour stale, unavailable, and more-than-24-hour expired states rendered without a Little Orbit crash. Expiry emptied display and profile preferences and removed `me.webp`, `partner.webp`, and both `.pending` files. At 150% font size, all relationship values and a long countdown remained reachable with the standard Wear scroll gesture.
- A disposable API 30 phone emulator opened the watch installer in automatic and manual modes. Discovery failure exposed current manual host, pairing-port, and connection-port fields; artifact failure exposed an explicit retry; the install action remained disabled without a verified APK. A synthetic six-digit code disappeared after `onStop` and was absent from the app-private file search after resume.
- The updated asynchronous photo path now holds the relationship guard through final thumbnail replacement. A concurrent purge therefore either rejects the callback before write or deletes the completed bounded write immediately after it; names, display values, and missing-photo clears use the same ordering rule.
- The paired smoke matrix passed on API 29, API 30, API 36 phone, and wide API 36 tablet after two real attachment-preview races were repaired: Markwon could request a private image before its authorized allowlist was ready, and concurrent image downloads could disagree while creating the same cache directory. Every final run rendered both sanitized inline PNG/GIF assets. The wide tablet retained its bounded content and persistent navigation rail.
- A later API 36 run signed in as the second disposable partner and opened the document plus both sanitized images created by the first account. While that partner remained on the library, the first account created another document; the foreground directory refresh displayed it within the seven-second test window. The client suppresses identical snapshots so the bounded five-second refresh does not replace an unchanged library, and only the exact allowlisted `relationship_inactive` code can stop refresh and trigger local relationship cleanup.
- The physical Pixel 8 Pro then signed in as that second disposable partner and opened the first partner's shared document with its synchronized Markdown body. The first physical run exposed an ambiguous repeated smoke-fixture title rather than an application failure; attachment seeds now record a unique ignored title and the Android harness opens that exact document. A repeat reached the lock screen before completing, so the physical attachment path remains unclaimed. The same corrected harness completed the full partner, inline PNG/GIF, preview, Edit, and Markdown-dock journey on API 36 without a Little Orbit fatal error.
- Cleanup verified the isolated Compose project name before removing its containers, networks, and three named volumes. The smoke package was removed from both the Pixel and emulator, the emulator was stopped, captured device state and disposable credentials were cleared to zero bytes, and the normal Pixel package remained installed.
- The mobile instrumentation suite passed all eleven cases on API 36. One earlier run was covered by an Android System UI ANR dialog; after the system process recovered, the isolated drawer case and the complete suite both passed. The data instrumentation suite also passed.
- A clean round API 34 Wear emulator installed and cold-launched Wear version code 16 named `1.0.0-rc.14` in 0.928 seconds without a Little Orbit fatal error. Its launcher showed the explicit unavailable state, and Wear OS discovered the tile and complication services.
- The current-source signed candidates were rebuilt after the attachment fixes. Independent `aapt` and `apksigner` inspection confirmed phone code 19/min API 29, Wear code 16/min API 30 with the required watch feature, target API 36, exact manifest byte counts and hashes, and the shared pinned certificate. On API 36, signed RC13 code 18 installed, upgraded directly to signed RC14 code 19 without data removal, and cold-launched successfully. RC14 was subsequently published with compatibility floor 6 and no enforcement instant; direct ADB replacement does not claim the in-app updater path.

The physical Pixel 8 Pro has partial isolated-package evidence for partner document discovery as described above. The screen locked before the corrected full run completed, and neither the Pixel Watch 3 nor Motorola tablet was connected for this final pass. The production app was not replaced, so the complete physical release gate remains open.

## Docker, production, and retention evidence

- Docker Desktop recovered without factory reset or data cleanup, then upgraded to 4.91 with Engine 29.8. The data VHD, images, containers, and named volumes were preserved. The later `sailor-ingest.sock` screenshot was a stale Desktop frontend dialog while the engine and all Little Orbit services remained healthy.
- Coordinated age-encrypted PostgreSQL and attachment backups completed, and an isolated restore drill recovered the expected privacy-safe metadata and six attachment files while restoring zero raw location rows.
- Production reached Alembic head `0023`. Migration `0023` shortened any legacy raw-location deadline to `recorded_at + 23 hours 55 minutes`; a disposable rehearsal verified the backfill before deployment, and the production aggregate has zero rows past that margin or the absolute 24-hour ceiling. API and worker run under distinct restricted database roles. Machine-controlled database and application secrets were rotated, all prior sessions and one-use tokens were revoked, administrator MFA was cleared for re-enrollment, and stored push ciphertext was removed. The Gmail app password was not rotated.
- The rebuilt API and worker passed health checks locally and through `https://lil-orb.pax-kun.com`. Three samples observed immediately before a forced sweep were at most 1 minute 41 seconds past the deliberately early 23-hour-55-minute deadline and only 23 hours 57 minutes old. A forced maintenance cycle under the restricted worker role completed successfully and reduced the privacy-safe raw-location aggregate to zero rows past either declared expiry or the absolute 24-hour ceiling.
- Global question coverage contains five general and one consent-gated question for each of today plus seven future UTC dates. FCM credentials are absent, so first-party polling remains authoritative and physical content-free push delivery is not claimed.
- The published phone APK is 38,224,790 bytes with SHA-256 `4d0f2a636b04523c91bdb0ccf134302ebdf95328b312f7c112e98664654be8bb`. The published Wear APK is 14,188,854 bytes with SHA-256 `6ce385654e323dfdcad9d5b6dec000a4f2549568d30b604ce6ea1400d642f0b6`. Both use certificate SHA-256 `43e83a420c7496ce9121339ab5bd6b01a6357161a83a95042ace56855bd89337`. Public metadata reports phone code 19, Wear code 16, and compatibility floor 6. Complete public downloads reproduced both hashes; 1,024-byte requests returned `206`, exact `Content-Range`, immutable caching, matching ETags, and checksum headers. API 36 also installed signed RC13 code 18, replaced it with this exact RC14 phone candidate, and cold-launched successfully.

## Open release gates

- Revoke active and idle note and notification WebSockets and observe terminal closure within 30 seconds.
- The fixed Caddy-to-API network identity and fail-closed header check are configured and unit tested. Prove through the deployed public route that distinct clients retain distinct rate-limit buckets while a direct spoofed header is ignored.
- Exercise administrator TOTP replay, recovery-code reuse, abandoned replacement expiry, and replacement on the deployed owner-console flow.
- Rehearse migration `0017` and a rollback on a disposable restored backup.
- Rotate the Gmail SMTP app password, re-enroll and exercise owner TOTP/recovery, configure Firebase credentials, and test content-free FCM wake delivery on two physical installations. RC14 therefore uses authoritative first-party polling. Browser automation is currently unavailable because the local control kernel cannot initialize; no substitute blank browser session was used for credential work.
- Validate TLS renewal and recovery in Nginx Proxy Manager, then enable and verify HSTS.
- Complete the existing two-physical-phone and phone/watch end-to-end release gate.
- Exercise the published phone and Wear update path, then verify urgent Data Layer purge, delayed-record rejection, launcher profile synchronization, tile, complication, current-version detection, fresh pairing, remembered reconnect, and APK transfer on the physical Pixel 8 Pro and Pixel Watch 3.
- Render the compact and expanded home widget through a real launcher at minimum, resized, stale, signed-out, and offline states; unit and lint checks do not replace this visual gate.
