{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://lil-orb.pax-kun.com/protocol/v3/location-batch.schema.json",
  "type": "object",
  "additionalProperties": false,
  "required": ["relationship_id", "samples"],
  "properties": {
    "relationship_id": { "type": "string", "format": "uuid" },
    "samples": {
      "type": "array",
      "minItems": 1,
      "maxItems": 48,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["sample_id", "recorded_at", "latitude", "longitude", "accuracy_m"],
        "properties": {
          "sample_id": { "type": "string", "format": "uuid" },
          "recorded_at": { "type": "string", "format": "date-time" },
          "latitude": { "type": "number", "minimum": -90, "maximum": 90 },
          "longitude": { "type": "number", "minimum": -180, "maximum": 180 },
          "accuracy_m": { "type": "number", "exclusiveMinimum": 0, "maximum": 1000 }
        }
      }
    }
  }
}
